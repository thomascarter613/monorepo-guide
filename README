# Building the Ultimate Monorepo Starter from First Principles

*A tutorial, engineering log, architecture journal, and reproducible build from an empty directory to a production-grade polyglot workspace.*

This repository is the canonical implementation and engineering record for the series. We build the starter and the written record together: each article introduces a real repository increment, preserves the evidence that shaped it, verifies the result, and leaves a checkpoint the next increment can build on.

## Start here

- `articles/` contains the reader-facing tutorial + engineering-log installments.
- `docs/series-roadmap.md` contains the evolving curriculum and article sequence.
- `docs/series.yaml` is the machine-readable series/increment index.
- `docs/ENGINEERING.md` defines the engineering protocol used for increments.
- `docs/README.md` explains the engineering-record directories.
- `docs/architecture/definition-of-ultimate.md` defines the properties the finished starter must prove.
- `docs/architecture/system-model.md` defines the cross-tool repository-platform vocabulary.
- `docs/architecture/requirements-ledger.md` turns those properties into stable, testable product requirements and evidence obligations.
- `docs/architecture/authority-map.md` assigns provisional concern-by-concern ownership and defines prohibited shadow authorities.
- `docs/architecture/decision-system.md` defines how consequential architecture decisions, experiments, reconsideration criteria, and supersession work.

## Engineering record

The documentation system intentionally separates current architecture from historical evidence:

- `docs/requirements/` — what a specific increment intended to make true;
- `docs/journal/` — assumptions, experiments, failures, corrections, and conclusions;
- `docs/decisions/` — consequential architecture decisions and their historical rationale;
- `docs/experiments/` — standalone consequential/reusable experiment records when a journal section is insufficient;
- `docs/verification/` — requirement-by-requirement evidence;
- `docs/artifacts/` — raw evidence too verbose for journals/experiments;
- `docs/architecture/` — how the repository and current product contract work now.

The governing principle is simple:

> Preserve what we knew at the time. Document what we know now separately.

## Current checkpoint

We are reviewing **Article 4 — Reversible Architecture: ADRs, Experiments, and Exit Criteria**.

Article 4 formalizes how architecture changes without rewriting history:

- ADRs have stable `ADR-NNNN` identities and explicit Proposed → Accepted/Rejected/Withdrawn → Superseded semantics;
- accepted ADR substance is historical, while narrow supersession/link metadata may be added later;
- current-state architecture remains separate from decision history;
- experiments produce evidence but do not make decisions;
- standalone `EXP-NNNN` records are used only when evidence is consequential/reusable enough to deserve a separate identity;
- material ADR assumptions and reconsideration criteria are explicit;
- decision status is kept separate from product-requirement evidence state.

The Article 3 authority model is now represented by independently reversible ADRs rather than one coupled “use this stack” decision.

Important deliberate non-claims:

- no tool implementation has been proven merely because its ADR is Accepted;
- no fake standalone experiment was created just because the template now exists;
- `UMS-GEN-003` remains Partial until the first **real** superseding ADR/migration provides the remaining evidence.

The Article 4 branch starts from immutable mainline commit:

`553f457b01d554429f7117f1e2ca824386835e37`

## Next after acceptance

**Article 5 — Building the Smallest Useful Repository Contract**

Article 5 begins Arc II — Repository Kernel. It will turn the accepted first-principles architecture into the smallest useful root contract, including source-of-truth conventions, root metadata/documentation entry points, the first machine-verifiable invariants, and the smallest durable repository-owned project identity representation justified by the requirements/ADRs.

### Historical note

The first Article 0/1 drafts began before the engineering-record protocol was merged to `main`. Their historical files disclose the retrospective portions. The stacked Article 0/1 merge sequence also demonstrated why branch names are insufficient as permanent checkpoint identities; the exact immutable checkpoint commits are recorded in `docs/series.yaml` and `articles/README.md`.
